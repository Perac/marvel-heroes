<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph.xml"
    app:startDestination="@id/listFragment">

    <action
        android:id="@+id/open_hero_details"
        app:destination="@id/detailsFragment"
        app:enterAnim="@anim/slide_in"
        app:exitAnim="@anim/fade_out"
        app:popEnterAnim="@anim/fade_in"
        app:popExitAnim="@anim/slide_out" />

    <fragment
        android:id="@+id/listFragment"
        android:name="com.perac.marvelheroes.ui.herolist.MarvelHeroListFragment">

        <action
            android:id="@+id/open_search"
            app:destination="@id/searchFragment"
            app:enterAnim="@anim/slide_in"
            app:exitAnim="@anim/fade_out"
            app:popEnterAnim="@anim/fade_in"
            app:popExitAnim="@anim/slide_out" />
    </fragment>

    <fragment
        android:id="@+id/detailsFragment"
        android:name="com.perac.marvelheroes.ui.herodetails.MarvelHeroDetailsFragment">
        <argument
            android:name="heroId"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.perac.marvelheroes.ui.search.MarvelHeroSearchFragment" />
</navigation>